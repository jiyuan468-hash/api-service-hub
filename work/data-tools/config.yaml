# Data Processing API Configuration

auth:
  api_key: "your-api-key-here"  # Change this in production!

max_file_size_mb: 20

storage_dir: "./storage"

server:
  host: "0.0.0.0"
  port: 8001
  workers: 4
